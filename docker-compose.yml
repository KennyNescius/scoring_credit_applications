version: "3.8"

services:
  credit-scoring:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./natija:/app/natija
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
